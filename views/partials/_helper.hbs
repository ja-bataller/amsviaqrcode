<script>
    const getDate = () => {
        let today = new Date();
        let dd = String(today.getDate()).padStart(2, "0");
        let mm = String(today.getMonth() + 1).padStart(2, "0"); //January is 0!
        let yyyy = today.getFullYear();
        today = mm + "-" + dd + "-" + yyyy;
        
        return today;
    }

    function time12h() {
        let date = new Date()
        var hours = date.getHours();
        var minutes = date.getMinutes();
        var ampm = hours >= 12 ? 'PM' : 'AM';
        hours = hours % 12; // change to 24 if 
        hours = hours ? hours : 12; // the hour '0' should be '12'
        minutes = minutes < 10 ? '0'+minutes : minutes;
        var strTime = hours + ':' + minutes + ' ' + ampm;
        return strTime;
    }


    function time24h() {
        let date = new Date()
        var hours = date.getHours();
        var minutes = date.getMinutes();
        var seconds = date.getSeconds();
        // var ampm = hours >= hours ? 'PM' : 'AM';
        hours = hours % 24; // change to 24 if 
        hours = hours ? hours : 12; // the hour '0' should be '12'
        minutes = minutes < 10 ? '0'+minutes : minutes;
        var strTime = hours + ':' + minutes + ':' + seconds;
        return strTime;
    }

    
    //alertMessage('error', data.error)
    //alertMessage('success', data.message)

   function alertMessage (status, message){

      if(status == 'success')
      {
        iziToast.success({
            title: message,
            position: "topCenter",
            timeout: 3000,
        });
      }else{
          iziToast.error({
            title: message,
            position: "topCenter",
            timeout: 3000,
        });
      }
   
   }

// console.log(time12h(), getDate());

/* 
    status = done 
       - Error message You cannot enter twice a day please contact your admin
    status active 
        - insert to time_out the time 
*/
</script>